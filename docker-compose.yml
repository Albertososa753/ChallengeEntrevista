services:
  db:
    image: postgres:bullseye
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_PASSWORD: secreto
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
